# Language Matching Rule Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a global rule that makes AI mirror the user's language in all natural-language responses.

**Architecture:** Single rule file with `alwaysApply: true` and `<HARD-GATE>` enforcement. Follows the same pattern as `debug-confirmation-policy.md`.

**Tech Stack:** Markdown rule file

---

### Task 1: Create Language Matching Rule

**Files:**
- Create: `.agent/rules/language-matching.md`

**Reference:** `.agent/rules/debug-confirmation-policy.md` (for format/structure)

- [ ] **Step 1: Create the rule file**

Create `.agent/rules/language-matching.md` with the following exact content:

```markdown
---
description: Match response language to user's language
alwaysApply: true
---

# Language Matching Policy

<HARD-GATE>
You MUST respond in the same language the user is writing in.

## Rules

1. **Detect** — Identify the language of the user's most recent message.
2. **Mirror** — Use that language for ALL natural-language content: explanations, descriptions, questions, analysis, summaries, and conversation.
3. **Switch dynamically** — If the user switches language mid-conversation, switch immediately from your next response onward.

## English Exceptions

The following ALWAYS stay in English regardless of user language:

- Code blocks, variable names, function names, class names
- Commit messages
- File names and file paths
- Branch names
- Technical terms with no widely-used equivalent (e.g., "refactor", "deploy", "middleware", "webhook")

## DO NOT:
- Respond in English when the user writes in another language
- Mix languages in a single response (except for English exceptions above)
- Default to English because "it's a technical topic"
- Ignore language switches mid-conversation
</HARD-GATE>
```

- [ ] **Step 2: Verify file was created correctly**

Run: `cat .agent/rules/language-matching.md`
Expected: File content matches the above, with proper YAML frontmatter and HARD-GATE tags.

- [ ] **Step 3: Verify rule is loadable alongside existing rules**

Run: `ls -la .agent/rules/`
Expected: Three rule files listed:
- `debug-confirmation-policy.md`
- `git-policy.md`
- `language-matching.md`
- `superpowers.md`

- [ ] **Step 4: Commit (if auto_commit enabled)**

Check `.agent/config.yml` for `auto_commit` setting:
- If `auto_commit: true` (default):
```bash
git add .agent/rules/language-matching.md
git commit -m "feat: add language matching rule to mirror user's language"
```
- If `auto_commit: false`: skip commit and staging. Print: "Skipping commit (auto_commit: false)."

---

## Verification Plan

### Manual Verification
1. Open a new AI chat session in a project that includes this rule
2. Send a message in Vietnamese → AI should respond entirely in Vietnamese
3. Send a follow-up in English → AI should switch to English
4. Ask a technical question in Vietnamese → AI should explain in Vietnamese, but code blocks remain in English
5. Verify commit messages generated by AI are still in English
